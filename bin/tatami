#!/usr/bin/env bash

function main {
  case "${1:-}" in
    k*|helm|kustomize|linkerd|glooctl)
      env KUBECONFIG="$HOME/.kube/${0##*/}.conf" "$@"
      ;;
    *)
      env KUBECONFIG="$HOME/.kube/${0##*/}.conf" k "$@"
      ;;
  esac
}

main "$@"
