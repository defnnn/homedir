#!/usr/bin/env bash

function main {
  local nm_role

  if [[ -n "${VAULT_DEFAULT_ROLE:-}" ]]; then
    set -- "role=${VAULT_DEFAULT_ROLE}" "$@"
  fi

  exec vault login -method=oidc "$@"
}

source sub "$0" "$@"
