#!/usr/bin/env bash

function main {
  set -efu

  if [[ "$#" == 0 ]]; then
    echo "syntax: $0 SERVICE [jq...]" 1>&2
    return 1
  fi

  local nm_service="$1"

  if [[ "$#" == 0 ]]; then
    set -- .
  fi

  chamber export --format json "${nm_service}" | jq -r "$@"
}

main "$@"
