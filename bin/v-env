#!/usr/bin/env bash

function main {
  if [[ -z "${VAULT_TOKEN:-}" ]]; then
    export VAULT_TOKEN="$(pass vault-token)"
  fi
  exec envconsul -no-prefix -upcase -sanitize -once -log-level ERR -secret "$@"
}

source sub "$0" "$@"
